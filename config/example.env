# Dokploy Enhanced - Example Environment Configuration
# Copy this file to .env and modify as needed

# =============================================================================
# Docker Image Configuration
# =============================================================================

# Docker image version (use 'latest' for most recent build)
DOKPLOY_VERSION=latest

# Docker registry (default: ghcr.io/amirhmoradi)
# DOKPLOY_REGISTRY=ghcr.io/amirhmoradi

# Docker image name (default: dokploy-enhanced)
# DOKPLOY_IMAGE=dokploy-enhanced

# =============================================================================
# Network Configuration
# =============================================================================

# Dokploy web interface port (default: 3000)
# DOKPLOY_PORT=3000

# Docker Swarm advertise address
# Leave empty for auto-detection, or set to your server's private IP
# ADVERTISE_ADDR=192.168.1.100

# Additional Docker Swarm init arguments
# Useful to avoid CIDR conflicts with cloud provider VPCs
# DOCKER_SWARM_INIT_ARGS="--default-addr-pool 172.20.0.0/16 --default-addr-pool-mask-length 24"

# =============================================================================
# Storage Configuration
# =============================================================================

# Data directory for Dokploy configuration and certificates
# DOKPLOY_DATA_DIR=/etc/dokploy

# Backup directory
# BACKUP_DIR=/var/backups/dokploy

# =============================================================================
# Database Configuration
# =============================================================================

# Custom PostgreSQL password (auto-generated if not set)
# POSTGRES_PASSWORD=your-secure-password-here

# =============================================================================
# Installation Options
# =============================================================================

# Skip Docker installation if already installed
# SKIP_DOCKER_INSTALL=false

# Skip Traefik installation (if using external reverse proxy)
# SKIP_TRAEFIK=false

# Force installation even with warnings
# FORCE=false

# Dry run mode - show commands without executing
# DRY_RUN=false

# Enable debug output
# DEBUG=false
